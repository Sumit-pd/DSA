#include <bits/stdc++.h> 
vector < vector < int >> shortestJobFirst(int n, vector < int > & processId,
    vector < int > & arrivalTime, vector < int > & burstTime) 
{
    vector<vector<int>> processDetails(n) ;
    for (int i = 0; i < n; i++)
    {
        proces
    }
    
    
}